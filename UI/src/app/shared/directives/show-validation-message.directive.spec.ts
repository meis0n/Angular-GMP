import { ShowValidationMessageDirective } from './show-validation-message.directive';

describe('ShowValidationMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowValidationMessageDirective();
    expect(directive).toBeTruthy();
  });
});
